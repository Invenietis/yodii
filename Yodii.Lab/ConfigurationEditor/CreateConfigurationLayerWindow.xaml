<Window
    x:Class="Yodii.Lab.ConfigurationEditor.CreateConfigurationLayerWindow"
    x:ClassModifier="internal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    SizeToContent="Height"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
    Title="Add new configuration layer"
    Height="200"
    Width="250">
    <StackPanel Margin="5pt">
        <TextBlock Margin="0,5pt">Layer name (optional) :</TextBlock>
        <TextBox
            Margin="0,5pt"
            x:Name="NewLayerName" />
        <DockPanel>
            <Border
                BorderThickness="0,1pt,0,0"
                BorderBrush="DarkGray"
                DockPanel.Dock="Bottom"
                Padding="5">
                <StackPanel
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
                    <Button
                        IsDefault="True"
                        Margin="3pt,0"
                        Click="OkButton_Click">Add</Button>
                    <Button
                        IsCancel="True">Cancel</Button>
                </StackPanel>
            </Border>
        </DockPanel>
    </StackPanel>
</Window>
